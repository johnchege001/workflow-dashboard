import { ResetPasswordView } from "@/components/auth/reset-password-view";

export const metadata = {
  title: "Reset Password / Outsourcelab",
};

export default function ResetPasswordPage() {
  return <ResetPasswordView />;
}
